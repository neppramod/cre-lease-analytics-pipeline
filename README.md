# CRE Lease Analytics Pipeline (cre-lease-analytics-pipeline)

A lightweight, deterministic, high-throughput ingestion pipeline designed to extract structural data from unstructured 
Commercial Real Estate (CRE) Triple-Net (NNN) lease agreements, map them to standard data schemas, and publish structured transactional events downstream.

# Why This Problem ?

In the Commercial Real Estate (CRE) industry, critical operational milestones, financial escalations, 
and risk management criteria are locked inside dense, unstructured PDF legal documents (Leases, Amendment Riders, Rent Rolls). 
Manually abstracting these elements introduces operational latency, human error, and massive data silos.

# Who It Serves ?

Asset Managers & Underwriters: Needs real-time visibility into financial cash flow steps and lease types across mixed-use property portfolios.
Property Operations / Compliance: Requires strict tracking of critical dates (e.g., Renewal deadlines) to prevent the loss of contractual options.

# Domain Research & Key Hypothesis

- **Hypothesis:** Unstructured lease PDFs can be parsed and normalized into a structured JSON format using deterministic extraction logic.
- **Key Entities:** Landlord, Tenant, Expiration Date, Lease Type (NNN/Gross), Escalation Clauses.

# Execution Steps V1

- Run the Spring Boot application

```shell
# On macOS / Linux (From project root directory, execute following command):
./mvnw spring-boot:run
```

# On Windows (Command Prompt):

```shell
mvnw.cmd spring-boot:run
```

- Using another terminal execute `runtests.sh` bash command (from inside src/main/resources/scripts/ directory) (to call the API for extracting relevant information from the PDF documents)

```shell
./src/main/resources/scripts/runtests.sh                
```

# Execution Step V2

You can also run the jar directly.

```shell
# 1. Package the codebase (skipping test sweeps for immediate launch)
./mvnw clean package -DskipTests

# 2. Run the production artifact directly via the JVM runtime
java -jar target/pdfreaderex-0.0.1-SNAPSHOT.jar
```

![Run Tests](images/run_tests_curl.png){width=40%}

# Classes

- PDFDocumentReader
  - Service to Read a PDF file and extracts text content using Apache PDFBox and returns list of Document objects
- DeterministicLeaseParser
  - Service to extract key information from the list of Document objects that was extracted using PDFDocumentReader
    - As an example, I am extracting information regarding Lanlord, Tenant and finally lease expiration date
- PDFReaderController uses above two services to read in the document name (examples are sample1, sample2, sample3), extract the text and return a JSON response with the captured fields
  - There are some duplicates here for the filename and passing them again. This is to simulate that we have list of file names, that is ready to be parsed (probably names can come from DB, Kafka event that this worker node reads etc.)
- LeaseData (model class) to capture the parsed information as an object

## Utility Clases

- PDFGeneratorUtility 
  - Apart from above core classes, I also have this utility class to generate simple PDFs to show variations in the documents. Although the first document is a free sample (not generated by this utility)
  - This utility can be run separately using TestPDFGeneratorUtilty

# Test Classes

- DeterministicLeaseParserTest: 
  - This is the main test class for the DeterministicLeaseParser. It reads in a sample PDF file and verifies the extracted fields

# Kafka (LeaseStreamingSimulationTest)

- I assumed we have the PDFs from some source. However, once we extract the necessary fields. I wanted to simulate the message is ready for further processing (can be used by various consumers)
- I use a simulated Kafka queue (as part of testing) that starts a lightweight Kafka broker, and verifies the message transfer through Kafka Producer/Consumer workflow
- The extracted object data is serialized into JSON, passed through Kafka queue, consumed by a consumer and deserialized back to LeaseData and verified the original data

To run the test, you can use following command

```shell
$ ./mvnw clean test
```

![Kafka Deserialization Log](images/deserialized_data_in_consumer_log.png){width=40%}

# Design Choices

After doing some research regarding CRE domain, it seemed I could build this project with some research on the key technologies. 

**Why I choose a PDF parsing service ?**

I wanted to create something that would work. There were couple of other ideas I had thought over (like showing locations for properties based on certain criteria and integrated a map, or parsing a big set of data to show various metrices). 

However, I leaned towards this project, because I could have something that would work and I can build iteratively. E.g. I initially researched just parsing the PDF (I had experience with IText, but used PDFBox, because I wanted to try something new, and it seemed popular to use), then added the web service component and then finally I could expand it to add a new workflow (Kafka).  

**What I learned from the research ?**

Commercial Real Estate (CRE) is an incredibly deep domain with highly varied contract types, amendment riders, and local regulatory nuances. Given the 2-day timeline, I intentionally tightly scoped my domain research to **Triple-Net (NNN) lease frameworks** (tenants paying property taxes, liability and other operating overheads) - specifically focusing on pro-rata square footage calculations and critical renewal timeline triggers.

**The Strategy:**  
Rather than expanding the domain scope horizontally (which would introduce an analysis bottleneck), I focused on building a robust, end-to-end data pipeline infrastructure capable of parsing a singular, high-value asset class perfectly.

**Future Horizon:**  
For a longer-term production rollout, I would spend more time embedding deeper into adjacent CRE legal constructs (such as absolute gross leases, co-tenancy clauses, and sub-leasing rights). Because the current code architecture is built with isolated, modular parsing services, expanding the data model to accommodate these additional domain variations in the future can be done seamlessly without requiring a rewrite of the core streaming pipeline infrastructure."

# Usage of AI

AI was part of the design and development from the start. 

## Design Phase

- During the analysis and research phase I used multiple AI tools (Google AI mode, ChatGPT) to understand the domain

- Based on the insights and deep dive on various termiologies, I was able to narrow down the design

- After that, I went through technology selection part. E.g. IText and other PDF parsers vi PTFBox. I also learned, it was part of the spring's initializer project (this made it natural for me to quickly spin up a SpringBoot project and test it)

- During the design phase I also though about using LLM (spring has APIs) to do the actual parsing. However, this would limit my scope on the project being testable. I am already using Claude Code and Ollama (with various local agents) for coding. However, deployment and testing (taking long time), and Hallucination issue would require more time to work with the proper agent. However, this would be where I would take the project next and see how much success I would get. Also, I would definitely lean towards AI at least after parsing the PDF to extract the fields (e.g. dates) instead of using a regex parser (which requires exact format, which is ok for demenstration purposes, but would needed to be expanded for production)

## Coding

- Since, I wanted to be in full control of what I was creating, before working on the code, I went through my notes to make sure I was using correct tool for the project

- I did not want to over engineer or under engineer the project and also did not want to include tools that would add no value to the project (that's why I restricted the project to a single focused task). I could expand on UI part (for uploading the documents), or databases, deploy to cloud environment etc. However, these part would be very much dependent on specific use cases of each organization. Therefore, after selecting what technologies I wanted to use, what project I was going to work on, I used AI (claude code and Google AI mode) to help me through various aspects (e.g. what packages to use). I then created the project (using spring initializer)

- I then worked through the basic code to parse the PDF, and used the project's documentation to get a working parser. 

- I used AI's help to create a shell script to test PDF generation

- Once the parsing part was done, I tought about integrating Kafka as another service. However, using it as a integration test made more sense to show the continuation of the complete workflow. Data comes in, the service parses the data and sends it to another process (or processes) in the pipeline

- I had worked on Kafka some time back, and since then there has been updates in various packages and various APIs I had last used had been depreciated E.g. JsonSerializer / DeSerializer (to serialize Key/Value for a Kafka message) has been depreciated. I had to use the AI agent to go through few iterations (back and forth) to get the code the way I wanted it to work.  E.g. The AI agent would introduce JsonSerializer/JsonDeserializer, I see the error and I had to ask it to use StringSerializer instead, and when few things broke, it would again go back to using JsonSerializer/JsonDeserializer. This part, where making sure AI agent works for the user and the actual use case is very important. This has been one of the things that I need to iterate a lot with AI agents. As part of integrating AI agents, I have seen when I become rigid at some point (it will explore more) and when it gets close to the goal, I can help it more by explaining what is the current state of the API and so on to get what I exactly want. E.g. In this project, for some APIs it was passing incompatible objects. I had to go through the API and guide it. E.g. "Please use ObjectMapper or compatible API for the parameter to abc() function", "The xyz() method is depreciated since version 2.0, could you explore on ABC etc."

- AI also helped me with insights on certain things. E.g. I used it to explain certain parts of the code, and when it changed the code I would ask the reasons, why it did. Since, I worked in small increments, I did not have the issue of it changing too much code at once (Also I had my code pushed to git, I did not have much issues, given the scope of the project).

# Assumptions

- I expect data/input (I have used few sample pdfs) to be part of another process. I did not build input methods to get the PDFs (I use few classes to create the PDF only for demenstration purposes)

- I am testing Kafka queue and expect the data will flow to correct channels, given the proper topic

- PDF is a difficult document to parse (because every tenant has their own standards) and they may not have all the fields populated or named in a consistent manner. The PDF parser created in the project is only used for demenstration purpose. A production environment would require more work on the parsing side. E.g. Adding more rules to capture a wide range of possible words, Use an OCR library, if the PDF is saved as an image and as mentioned above use an AI parser, which can create meaning out of unstructured texts.

# Changes

- As explained above, I would definitely look into using a LLM tool in the PDF parsing process. And if I have a very comparable PDF parsing library itself, compare and contrast between both (manual parsing vs LLM parsing)

- Would definitely use LLM tool to extract the correct fields after parsing, as the documents can have the given fields described in different contexts. A LLM tool can certainly do better than a parser on this

- By focusing heavily on the ingress/egress contract boundaries via Kafka, we've built a system where the parsing core can be swapped or upgraded with zero breaking changes to downstream consumer architectures.

# Production Ready Changes

## Future Production Containerization Strategy (Docker)

To transition this microservice from a local prototype into a globally scalable, cloud-native deployment topology, the next immediate phase is containerization.

### 1. Multi-Stage Dockerfile Optimization
Rather than packaging a heavy development environment, the production image will utilize a **multi-stage build** to optimize performance, minimize the attack surface, and keep the final image size under 150MB:

```dockerfile
# Stage 1: Build the optimized production artifact
FROM eclipse-temurin:17-jdk-jammy AS builder
WORKDIR /app
COPY . .
RUN ./mvnw clean package -DskipTests

# Stage 2: Minimal distroless runtime execution container
FROM eclipse-temurin:17-jre-jammy
WORKDIR /app
COPY --from=builder /app/target/pdfreaderex-*.jar app.jar
EXPOSE 8080
USER 1001
ENTRYPOINT ["java", "-jar", "app.jar"]
```

### 2. Multi-Container Orchestration (Docker Compose)
In an enterprise cloud profile, the in-memory Embedded KRaft broker would be pulled out. A `docker-compose.yml` manifest would orchestrate the distributed boundaries, linking the parsing microservice with an external resilient streaming cluster and a document staging database:

* **`cre-parser-service`:** Runs our Spring Boot ingestion app.
* **`kafka-broker`:** Dedicated KRaft-mode controller container image handling event-stream traffic.
* **`postgre-vector-db`:** Relational database container equipped with `pgvector` to store parsed text fragments once the system scales to an AI/LLM parsing approach.
